// Tailwind CSS v4 config (optional). Adjust theme or plugins here as needed.
/** @type {import('tailwindcss').Config} */
module.exports = {
  // Help the CLI find classes in your source
  content: [
    "./src/**/*.{html,js,ts}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};